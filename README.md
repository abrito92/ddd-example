# ddd-example
An Example of DDD structure for .NET API's

This project is to serve as an exemple structure. It also includes pipelines, for azure devops, to deploy, controll version and execute static tests. 
